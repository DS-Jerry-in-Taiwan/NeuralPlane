  AI 驅動型 Scrum 管理架構 MVP 規劃書

  1. 使用場景 (Usage Scenarios)
   * 場景 A：Sprint 規劃期 —— 組員討論出每週目標後，AI 自動將討論逐字稿或筆記轉化為標準化的 Task 卡片，並同步生成驗收清單 (DoD)。
   * 場景 B：開發執行期 —— 組員在 GitHub 進行代碼提交與 PR，系統自動同步進度至視覺化看板，無需人工手動更新「進行中」或「已完成」。
   * 場景 C：驗收與整合期 —— AI Agent 讀取 PR 內容，對照當初設定的驗收標準進行自動化審核，並產出整合測試報告，標記是否符合衝刺目標。

  2. 使用流程 (Workflow)
   1. 目標拆解 (Planning)：
       * 人類討論「大方向」與「週目標」。
       * AI 介入：將討論內容輸入，AI 在 Plane 自動建立 Issue，並在每個 Issue 描述中寫入「驗收標準 (DoD)」。
   2. 領票開發 (Execution)：
       * 組員在 Plane 領取任務，或直接在 GitHub 建立對應 Branch。
       * 自動同步：GitHub 與 Plane 雙向連結，只要有 Commit，Plane 看板自動更新進度。
   3. 自動驗收 (Validation)：
       * 組員發起 PR，AI Agent 自動讀取代碼，並與 Plane 上的「驗收標準」核對。
       * AI 在 GitHub 留言：「驗收標準 A 通過，標準 B 缺少單元測試...」。
   4. 彙整報告 (Reporting)：
       * Plane 根據 GitHub 數據自動生成週報、燃盡圖與整合測試通過率。

  3. 最小達成需求與衡量標準 (MVP Requirements & Metrics)

  最小達成需求 (Must-haves):
   * 自動進度追蹤：GitHub 的 PR 狀態 (Open/Merged) 必須 100% 同步至 Plane 看板。
   * 統一驗收模板：所有從 AI 建立的 Task 必須包含 [DoD] 區塊。
   * AI 留言審核：PR 建立時，AI 必須能讀取該任務的描述並給出初步審核意見。

  衡量標準 (Success Metrics):
   * 管理成本降低：組員每週手動更新看板與寫週報的時間減少 50% 以上。
   * 驗收透明度：100% 的任務在開發前就擁有明確、可追蹤的驗收準則。
   * 測試一致性：整合測試流程從「口頭討論」轉變為「AI 門檻機制」，遺漏驗收項的比例降低。

  4. 模組架構簡介與分工 (Architecture & Labor)

  ┌─────────────────────────┬────────────────────────┬────────────────────────────────────────────────────────────────┐
  │ 模組名稱                │ 負責工具               │ 主要分工                                                       │
  ├─────────────────────────┼────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 可視化中樞 (The Body)   │ Plane                  │ 存放 Epic/Cycle 目標、視覺化看板、燃盡圖、進度儀表板。         │
  │ 執行實體 (The Muscle)   │ GitHub                 │ 存放代碼、Issues、PR、觸發 GitHub Actions 執行 CI/CD。         │
  │ AI 邏輯引擎 (The Brain) │ Paca / MCP / GPT-4o    │ 負責：1. 拆票與寫 DoD 2. PR 代碼邏輯審核 3. 產出整合分析報告。 │
  │ 連接器 (The Nervous)    │ GitHub Sync / Webhooks │ 負責 Plane 與 GitHub 之間的即時狀態同步。                      │
  └─────────────────────────┴────────────────────────┴────────────────────────────────────────────────────────────────┘
  5. 案例說明 (Case Study)

  目標：本週衝刺「用戶登入 API 整合」

   * Step 1 (規劃)：
       * 組內討論出需要：資料庫串接、密碼加密、JWT 生成。
       * AI Agent 在 Plane 建立任務：[User Login API]。
       * AI 生成驗收標準：
           1. 密碼不可明文存儲 (DoD 1)
           2. 登入成功需回傳過期時間為 24h 的 Token (DoD 2)
           3. 需包含整合測試代碼 (DoD 3)
   * Step 2 (執行)：
       * 開發者 A 領取任務並在 GitHub 開發。
       * 當 A 提交第一個 Commit，Plane 看板自動將此票移入 In Progress。
   * Step 3 (驗收)：
       * 開發者 A 發起 PR。
       * AI Agent 介入：讀取 PR 代碼，發現「Token 過期時間設為 72h」，與 DoD 2 不符。
       * AI 在 GitHub 留言：「警告：DoD 2 驗收未通過，請修正過期時間。」
   * Step 4 (完成)：
       * 修正後 PR 合併，Plane 自動結案，並在週五自動產出：「本週用戶登入功能 100% 符合驗收標準，已成功整合。」

  ---

  如何在您的團隊落地？
   1. 基礎建設：花 30 分鐘部署一個 Plane 實例 (Docker)。
   2. 連結設定：在 Plane 設定中開啟 GitHub 同步，將目前的專案 Repo 綁定。
   3. Prompt 規範：建立一個簡單的 VERIFICATION_PROMPT，讓組員討論完目標後，直接貼給 AI 產生 Plane 任務卡片。
